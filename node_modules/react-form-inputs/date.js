/**
 * @flow
 * @jsx React.DOM
*/
var React = require('react');
var Base = require('./base');

var DateInput = React.createClass({displayName: "DateInput",
  render:function()                {
    return React.createElement(Base, React.__spread({},  this.props, {type: "date"}));
  }
});

module.exports = DateInput;
