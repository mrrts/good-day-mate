/**
 * @flow
 * @jsx React.DOM
*/
var React = require('react');
var Base = require('./base');

var Time = React.createClass({displayName: "Time",
  render:function()                {
    return React.createElement(Base, React.__spread({},  this.props, {type: "time"}));
  }
});

module.exports = Time;
