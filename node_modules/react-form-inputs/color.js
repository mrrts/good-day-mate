/**
 * @flow
 * @jsx React.DOM
*/
var React = require('react');
var Base = require('./base');

var Color = React.createClass({displayName: "Color",
  render:function()                {
    return React.createElement(Base, React.__spread({},  this.props, {type: "color"}));
  }
});

module.exports = Color;
