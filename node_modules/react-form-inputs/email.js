/**
 * @flow
 * @jsx React.DOM
*/
var React = require('react');
var Base = require('./base');

var Email = React.createClass({displayName: "Email",
  render:function()                {
    return React.createElement(Base, React.__spread({},  this.props, {type: "email"}));
  }
});

module.exports = Email;
