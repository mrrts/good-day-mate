/**
 * @flow
 * @jsx React.DOM
*/
var React = require('react');
var Base = require('./base');

var Datetime = React.createClass({displayName: "Datetime",
  render:function()                {
    return React.createElement(Base, React.__spread({},  this.props, {type: "datetime-local"}));
  }
});

module.exports = Datetime;
