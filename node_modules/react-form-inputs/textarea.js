/**
 * @flow
 * @jsx React.DOM
*/
var React = require('react');
var Base = require('./base');

var Textarea = React.createClass({displayName: "Textarea",
  render:function()                {
    return React.createElement(Base, React.__spread({},  this.props, {element: "textarea"}));
  }
});

module.exports = Textarea
