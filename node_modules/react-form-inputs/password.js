/**
 * @flow
 * @jsx React.DOM
*/
var React = require('react');
var Base = require('./base');

var Password = React.createClass({displayName: "Password",
  render:function()                {
    return React.createElement(Base, React.__spread({},  this.props, {type: "password"}));
  }
});

module.exports = Password;
