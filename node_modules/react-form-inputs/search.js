/**
 * @flow
 * @jsx React.DOM
*/
var React = require('react');
var Base = require('./base');

var Search = React.createClass({displayName: "Search",
  render:function()                {
    return React.createElement(Base, React.__spread({},  this.props, {type: "search"}));
  }
});

module.exports = Search;
