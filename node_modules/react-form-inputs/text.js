/**
 * @flow
 * @jsx React.DOM
*/
var React = require('react');
var Base = require('./base');

var Text = React.createClass({displayName: "Text",
  render:function()                {
    return React.createElement(Base, React.__spread({},  this.props, {type: "text"}));
  }
});

module.exports = Text;
