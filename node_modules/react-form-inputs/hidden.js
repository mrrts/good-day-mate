/**
 * @flow
 * @jsx React.DOM
*/
var React = require('react');
var Base = require('./base');

var Hidden = React.createClass({displayName: "Hidden",
  render:function()                {
    return React.createElement(Base, React.__spread({},  this.props, {type: "hidden"}));
  }
});

module.exports = Hidden;
